#!/usr/bin/env groovy

@Library('enlibs') _

pipeline {
    stages {
        stage("Prepare") {
            steps {
              script {
                enlibs.sayHello ""
              }
            }
        }
    }
}
