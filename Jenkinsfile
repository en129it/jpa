#!/usr/bin/env groovy

@Library('enlibs@master')_

pipeline {
    stages {
        stage("Prepare") {
            steps {
              script {
                enlibs.sayHello ""
              }
            }
        }
    }
}
